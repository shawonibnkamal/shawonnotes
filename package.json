{
  "devDependencies": {
    "@types/node": "^20.12.5",
    "prettier": "^3.2.5",
    "vitepress": "^1.0.2"
  },
  "scripts": {
    "prepare": "node ./convertJsonToMd.js && node ./generateIndexFiles.js",
    "dev": "npm run prepare && node ./generatePdfs.js && vitepress dev",
    "build": "npm run prepare && vitepress build",
    "preview": "vitepress preview"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.8.4",
    "cheerio": "^1.0.0",
    "google-it": "^1.6.4",
    "gray-matter": "^4.0.3",
    "md-to-pdf": "^5.2.4",
    "openai": "^4.91.1",
    "puppeteer": "^24.6.0",
    "serpapi": "^2.1.0"
  }
}
